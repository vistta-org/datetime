name: Project Validation
on:
  pull_request:
    types: [synchronize]
    branches:    
      - master
jobs:
  validation:
    uses: ./.gitea/workflows/validation.yml
    with:
      directory: ./packages/datetime